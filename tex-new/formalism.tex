% FORMALISM OVERVIEW
%
% PURPOSE: Define a logic-agnostic formal language for expressing embedded/avionics
% software requirements and test cases, so that coverage gaps can be detected
% mechanically. The ten Embraer requirements in req/ are the concrete test bed.
%
% AUTHORITATIVE REFERENCE: ../abstract-final.md (8-section spec).
% This file is the LaTeX rendering of that spec.
%
% KEY DESIGN PRINCIPLES:
%
% 1. LAYERS
%    Primitives (T, V) -> Entities & Dictionary -> Valuations & Traces ->
%    Expressions & Predicates -> Operations -> Temporal Constructs -> Requirements
%    -> Test Cases & Coverage
%
% 2. EXPRESSIONS ARE EVALUATED AT AMBIENT (rho, t)
%    Every expression is implicitly a function of trace rho and current time t.
%    e.g. prev(e) means Val_rho(e, t-1) where t is supplied by the surrounding
%    temporal operator, NOT by the operation call. This means operations do NOT
%    take t as an explicit argument -- t is implicit.
%
% 3. OPERATIONS DEFINE POINT-IN-TIME EFFECT PREDICATES
%    An operation such as calculate(e, expr) produces a predicate that holds at
%    the *current* t. The temporal extent (always, eventually, from t onwards)
%    is specified at the requirement level via temporal operators (always, triggered,
%    etc.), not inside the operation definition.
%    Example: always(calculate(e, 1.2 * prev(e))) expresses the recurrence
%    e(k) = 1.2 * e(k-1) for all k -- the always handles "for all time".
%
% 4. THE DICTIONARY IS OPEN
%    Entity types (Signal, Storage, Event, Channel, State, Value, Mapping, Abstract)
%    each declare: semantics, applicable operations, and admitted modifiers.
%    New types are added by extending the dictionary, not by changing the core.
%
% 5. COVERAGE IS DERIVED FROM REQUIREMENT STRUCTURE
%    MC/DC for ALL_OF conditions, per-element for ANY_OF, boundary tests for time
%    bounds, negative cases for guards, ordering violation tests for strict sequences.
%
% KNOWN OPEN ISSUES (see Notes section and todo.md):
%    - Modifiers need to be parameterised
%    - Discrete vs continuous time flavour handling
%    - Partiality of time functions (Prev at t=0)

\documentclass{article}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsthm}
\usepackage{tabularx}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{stmaryrd}
\usepackage[margin=2.5cm]{geometry}
\usepackage[normalem]{ulem}
\usepackage{nameref}

\newtheorem{definition}{Definition}

\title{PP1 - Vyhodnocování úplnosti testů vůči softwarovým požadavkům - formalismus}
\author{  }
\date{April 2026}

\begin{document}

\maketitle

\section{The primitives}

These primitive sets serve as the base that the rest of the formalism builds on. The primitive sets are the following:
\begin{itemize}
    \item $T_D$, $T_C$ -- the \textit{discrete time} and \textit{continuous time} sets.
          An infinite totally ordered set; the time set can have \textit{discrete} or \textit{continuous} flavour; exactly one flavour is chosen per requirement.
    \item $V$ -- the $Values$ set. These can be used to describe the values of entities.
\end{itemize}

\begin{definition}[Discrete time set]\label{def:time_discrete}
    The \emph{discrete time set} $T_D$ is totally ordered and countable.
    \[
        T_D = \mathbb{N}
    \]
\end{definition}

\begin{definition}[Continuous time set]\label{def:time_continuous}
    The \emph{continuous time set} $T_C$ is totally ordered and uncountable.
    \[
        T_C = \mathbb{R}_{\geq 0}
    \]
\end{definition}

\begin{definition}[Duration]\label{def:duration}
    A \emph{duration} is a non-negative element of the time set, representing the length of a time span:
    \[
        \Delta = T
    \]
    Thus $\Delta = \mathbb{N}$ for discrete time and $\Delta = \mathbb{R}_{\geq 0}$ for continuous time.
\end{definition}

\begin{definition}[Interval]\label{def:interval}
    An \emph{interval} is a pair of a start time point and a duration:
    \[
        \mathcal{I} = T \times \Delta
    \]
    An interval $(t, d) \in \mathcal{I}$ represents the time span $[t,\, t + d]$.
\end{definition}

\begin{definition}[Values]\label{def:values}
    The \emph{values set} $V$ is the union of the following sets:
    \[
        V = \mathbb{B} \cup \mathbb{R} \cup T \cup \mathcal{I}
    \]
    where $\mathbb{B} = \{\mathit{true}, \mathit{false}\}$ is the set of booleans, $\mathbb{R}$ is the set of real numbers, $T$ is the time set, and $\mathcal{I}$ is the set of intervals.
\end{definition}

\section{Entities and entity types}

An entity type is an item belonging to a set of predefined entity types.

\begin{definition}[Entity type set]\label{def:entity_type_set}
    \[
        T_\text{E} = \{ \text{Signal}, \text{Storage}, \text{Event}, \text{Channel}, \text{State}, \text{Value}, \text{Abstract}\}
    \]
\end{definition}

\begin{definition}[Entity name set]\label{def:entity_name_set}
    \[
        N_\text{E} \subseteq \Sigma^*
    \]

    , where $\Sigma$ is an alphabet.
\end{definition}

Entity type modifiers are properties which can be attached to
an entity of a particular type. Semantics are defined for each modifier.

Each entity type is compatible with only a subset of the entity modifiers.
Therefore we define the \textit{entity type modifiers function}.

\begin{definition}[Entity type modifiers function]\label{def:entity_type_modifiers_fn}
    \[
        M_\text{E}: T_E \rightarrow \mathcal{P}(M)
    \]

    , where $M$ is the set of entity type modifiers and $\mathcal{P}(M)$ is the power set of $M$.
\end{definition}

% \begin{definition}[Entity type]\label{def:entity_type}
%     \[
%         T_e
%     \]
% \end{definition}

An entity is something that is referenced in the requirement.
Formally, entity is defined as a four-item tuple.

\begin{definition}[Entity]\label{def:entity}
    \[
        e = (n_e, t_e, m_e, d_e)
    \]

    , where
    \begin{itemize}
        \item $n_e$ is the name of the entity; $n_e \in N_E$
        \item $t_e$ is the name of the entity; $t_e \in T_E$
        \item $m_e$ is the set of modifiers of the entity; $m_e \in T_E$, $m_e \subseteq M_E(t_e)$
        \item $d_e$ is the description of the entity; $d_e \in \Sigma^*$
    \end{itemize}
\end{definition}

\section{Valuations and traces}

\begin{definition}[Valuation]\label{def:valuation}
    A valuation is a function $\sigma : E \to V$ assigning a value to every entity.
    For entities of type \texttt{Event}, $\sigma(e) \in \{\mathit{true}, \mathit{false}\}$,
    where $\sigma(e) = \mathit{true}$ iff the event occurs at that time point.
\end{definition}

We write $\Sigma$ for the set of all valuations.

\begin{definition}[Trace]\label{def:trace}
    A trace is a function $\rho : T \to \Sigma$. We write $\rho(t)(e)$ for the value of entity $e$ at time $t$.
\end{definition}

We write $\Sigma^T$ for the set of all traces, i.e.\ the set of all functions from $T$ to $\Sigma$.

\footnote{This is standard function space notation: $\Sigma^T = \{\, \rho \mid \rho : T \to \Sigma \,\}$.}

\section{Expressions and predicates}

All expression functions defined in this section evaluate to a value in $V$.
Functions in \S\ref{subsec:time} (\nameref{subsec:time}) depend only on time points or intervals.
Functions in \S\ref{subsec:trace} (\nameref{subsec:trace}) additionally take a trace $\rho \in \Sigma^T$
and produce values that reflect the state of the system over time.

\subsection{Expressions}\label{subsec:expressions}

\subsubsection{Constants and arithmetic}\label{subsec:constants}

\begin{definition}[Constant]\label{def:constant}
    Any value $c \in V$ may appear as a constant expression.
\end{definition}

The standard arithmetic operators $+$, $-$, $\times$, $/$ are defined on numeric values ($\mathbb{R}$ and its subsets) in the usual sense.

\subsubsection{Time functions}\label{subsec:time}

The following functions operate on time points and intervals only; they do not depend on a trace.

The following two functions are defined for \textbf{discrete time only} ($T = T_D = \mathbb{N}$).

\begin{definition}[]\label{def:prev}
    $\mathit{Prev} : T_D \rightharpoonup T_D$ returns the preceding time step:
    \[
        \mathit{Prev}(t) \;=\; t - 1
    \]
    The function is partial: it is undefined at $t = 0$ since $t - 1 \notin T_D$.
\end{definition}

\begin{definition}[]\label{def:next}
    $\mathit{Next} : T_D \rightarrow T_D$ returns the following time step:
    \[
        \mathit{Next}(t) \;=\; t + 1
    \]
\end{definition}

\begin{definition}[]\label{def:reltime}
    $\mathit{RelTime} : T_D \times \mathbb{Z} \rightharpoonup T_D$ shifts a time point by $n$ steps, where $n \in \mathbb{Z}$ may be positive or negative:
    \[
        \mathit{RelTime}(t,\, n) \;=\; t + n
    \]
    The function is partial: it is undefined when $t + n < 0$ since $t + n \notin T_D$.
    Note that $\mathit{Prev}(t) = \mathit{RelTime}(t, -1)$ and $\mathit{Next}(t) = \mathit{RelTime}(t, 1)$.
\end{definition}

The following functions are defined for both time flavours.

\begin{definition}[]\label{def:diff}
    $\mathit{Diff} : T \times T \rightarrow \Delta$ returns the duration between two time points:
    \[
        \mathit{Diff}(t_1,\, t_2) \;=\; |t_1 - t_2|
    \]
\end{definition}

\begin{definition}[]\label{def:mk_interval}
    $\mathit{MkInterval} : T \times T \rightharpoonup \mathcal{I}$ constructs an interval from a start and end time point:
    \[
        \mathit{MkInterval}(t_1,\, t_2) \;=\; (t_1,\; t_2 - t_1)
    \]
    The function is partial: it is undefined when $t_1 > t_2$.
\end{definition}

\begin{definition}[Since-zero interval]\label{def:since_zero}
    $\mathit{SinceZero} : T \rightarrow \mathcal{I}$ constructs an interval from time zero to a given time point $t$:
    \[
        \mathit{SinceZero}(t) \;=\; (0,\; t)
    \]
\end{definition}

\begin{definition}[Interval start]\label{def:interval_start}
    $\mathit{Start} : \mathcal{I} \rightarrow T$ returns the start time point of an interval:
    \[
        \mathit{Start}((t,\, d)) \;=\; t
    \]
\end{definition}

\begin{definition}[Interval end]\label{def:interval_end}
    $\mathit{End} : \mathcal{I} \rightarrow T$ returns the end time point of an interval:
    \[
        \mathit{End}((t,\, d)) \;=\; t + d
    \]
\end{definition}

\begin{definition}[Interval duration]\label{def:interval_duration}
    $\mathit{Duration} : \mathcal{I} \rightarrow \Delta$ returns the duration of an interval:
    \[
        \mathit{Duration}((t,\, d)) \;=\; d
    \]
\end{definition}

\subsubsection{Trace functions}\label{subsec:trace}

Expressions are terms that evaluate to a value in $V$ given a trace $\rho$.

\begin{definition}[]\label{def:expr_val}
    For a trace $\rho \in \Sigma^T$, the function $\mathit{Val}_\rho: E \times T \rightarrow V$ denotes the value of entity $e \in E$ at time $t \in T$:
    \[
        \mathit{Val}_\rho(e,\, t) \;=\; \rho(t)(e)
    \]
\end{definition}

\begin{definition}[Event occurrence count]\label{def:evt_occ_count}
    For a trace $\rho \in \Sigma^T$ and an entity $ev \in E$ of type \texttt{Event},
    $\mathit{EvtOccCount}_\rho : E \times \mathcal{I} \rightarrow \mathbb{N}$ returns the number of occurrences of $ev$ within an interval $i \in \mathcal{I}$:
    \[
        \mathit{EvtOccCount}_\rho(ev,\, i) \;=\; \bigl|\{\, s \in T \mid \mathit{Start}(i) \leq s \leq \mathit{End}(i) \;\land\; \mathit{Val}_\rho(ev,\, s) = \mathit{true} \,\}\bigr|
    \]
\end{definition}

\begin{definition}[Last occurrences function]\label{def:last_occ}
    For a trace $\rho \in \Sigma^T$, an entity $ev \in E$ of type \texttt{Event}, a time point $t \in T$, and $n \in \mathbb{N}^+$,
    let $t_1 \leq t_2 \leq \cdots \leq t_n$ be the $n$ most recent occurrence times of $ev$ up to $t$, i.e.\ the $n$ largest elements of $\{\, s \in T \mid s \leq t \;\land\; \mathit{Val}_\rho(ev,\, s) = \mathit{true} \,\}$ sorted in ascending order.
    Then $\mathit{LastOcc}_\rho : E \times T \times \mathbb{N}^+ \rightharpoonup \mathcal{I}$ is defined as:
    \[
        \mathit{LastOcc}_\rho(ev,\, t,\, n) \;=\; \mathit{MkInterval}(t_1,\, t_n)
    \]
    The function is partial: it is undefined when fewer than $n$ occurrences of $ev$ exist up to $t$.

    \textit{Note:} The time of a single occurrence is obtained via $\mathit{LastOcc}_\rho(ev, t, 1)$, which returns an interval of duration $0$. The occurrence time is then $\mathit{Start}(\mathit{LastOcc}_\rho(ev, t, 1))$.
\end{definition}

\begin{definition}[First occurrences function]\label{def:first_occ}
    For a trace $\rho \in \Sigma^T$, an entity $ev \in E$ of type \texttt{Event}, a time point $t \in T$, and $n \in \mathbb{N}^+$,
    let $t_1 \leq t_2 \leq \cdots \leq t_n$ be the $n$ earliest occurrence times of $ev$ up to $t$, i.e.\ the $n$ smallest elements of $\{\, s \in T \mid s \leq t \;\land\; \mathit{Val}_\rho(ev,\, s) = \mathit{true} \,\}$ sorted in ascending order.
    Then $\mathit{FirstOcc}_\rho : E \times T \times \mathbb{N}^+ \rightharpoonup \mathcal{I}$ is defined as:
    \[
        \mathit{FirstOcc}_\rho(ev,\, t,\, n) \;=\; \mathit{MkInterval}(t_1,\, t_n)
    \]
    The function is partial: it is undefined when fewer than $n$ occurrences of $ev$ exist up to $t$.

    \textit{Note:} The time of the first occurrence is $\mathit{Start}(\mathit{FirstOcc}_\rho(ev, t, 1))$.
\end{definition}

\begin{definition}[Maximum value function]\label{def:max_val}
    For a trace $\rho \in \Sigma^T$ and an entity $e \in E$ with an ordered value domain,
    $\mathit{MaxVal}_\rho : E \times \mathcal{I} \rightarrow V$ returns the maximum value of $e$ over an interval $i \in \mathcal{I}$:
    \[
        \mathit{MaxVal}_\rho(e,\, i) \;=\; \sup\{\, \mathit{Val}_\rho(e,\, s) \mid s \in T,\; \mathit{Start}(i) \leq s \leq \mathit{End}(i) \,\}
    \]
\end{definition}

\begin{definition}[Minimum value function]\label{def:min_val}
    For a trace $\rho \in \Sigma^T$ and an entity $e \in E$ with an ordered value domain,
    $\mathit{MinVal}_\rho : E \times \mathcal{I} \rightarrow V$ returns the minimum value of $e$ over an interval $i \in \mathcal{I}$:
    \[
        \mathit{MinVal}_\rho(e,\, i) \;=\; \inf\{\, \mathit{Val}_\rho(e,\, s) \mid s \in T,\; \mathit{Start}(i) \leq s \leq \mathit{End}(i) \,\}
    \]
\end{definition}

\subsection{Predicates}\label{subsec:predicates}

A predicate is a function that evaluates to a value in $\mathbb{B}$.
We write $\mathbf{Pred}$ for the set of all predicates.

\begin{definition}[Boolean constants]\label{def:pred_bool}
    $\top \in \mathbf{Pred}$ and $\bot \in \mathbf{Pred}$ are the always-true and always-false predicates respectively.
\end{definition}

\begin{definition}[Comparison predicate]\label{def:pred_cmp}
    For expressions $a, b \in V$ and a comparison operator $\mathit{op} \in \{=,\, \neq,\, <,\, \leq,\, >,\, \geq\}$:
    \[
        \mathit{Cmp}(a,\, \mathit{op},\, b) \;=\; a \;\mathit{op}\; b
    \]
    The operators $<, \leq, >, \geq$ require $a, b$ to be from an ordered domain.
    Since $\Delta \subseteq \mathbb{R}$, durations are an ordered domain and duration comparison is a valid instance of $\mathit{Cmp}$.
\end{definition}

\begin{definition}[Negation]\label{def:pred_not}
    For a predicate $P \in \mathbf{Pred}$:
    \[
        \mathit{Not}(P) \;=\; \lnot P
    \]
\end{definition}

\begin{definition}[Conjunction and disjunction]\label{def:pred_allandany}
    For a finite set of predicates $C \subseteq \mathbf{Pred}$:
    \[
        \mathit{AllOf}(C) \;=\; \bigwedge_{P \in C} P \qquad
        \mathit{AnyOf}(C) \;=\; \bigvee_{P \in C} P
    \]
\end{definition}

\begin{definition}[Implication]\label{def:pred_implies}
    For predicates $A, B \in \mathbf{Pred}$:
    \[
        \mathit{Implies}(A,\, B) \;=\; A \Rightarrow B
    \]
    where $\Rightarrow$ is the standard logical implication.
\end{definition}

\begin{definition}[Quantifiers]\label{def:pred_quant}
    For a finite set $S$ and a predicate $P(x)$ parameterised by $x \in S$:
    \[
        \mathit{ForAll}(S,\, P) \;=\; \bigwedge_{x \in S} P(x) \qquad
        \mathit{Exists}(S,\, P) \;=\; \bigvee_{x \in S} P(x)
    \]
\end{definition}

The following predicates operate on intervals and durations.
Since $\Delta \subseteq \mathbb{R}$, duration comparison is a special case of $\mathit{Cmp}$ (Definition~\ref{def:pred_cmp}).

\begin{definition}[Interval inclusion]\label{def:pred_includes}
    Interval $i_1$ includes $i_2$ iff $i_2$ is entirely contained within $i_1$:
    \[
        \mathit{IntervalIncludes}(i_1,\, i_2) \;\Leftrightarrow\;
        \mathit{Start}(i_1) \leq \mathit{Start}(i_2) \;\land\; \mathit{End}(i_2) \leq \mathit{End}(i_1)
    \]
\end{definition}

\begin{definition}[Interval exclusion]\label{def:pred_excludes}
    Intervals $i_1$ and $i_2$ are exclusive iff they are completely disjoint:
    \[
        \mathit{IntervalExcludes}(i_1,\, i_2) \;\Leftrightarrow\;
        \mathit{End}(i_1) < \mathit{Start}(i_2) \;\lor\; \mathit{End}(i_2) < \mathit{Start}(i_1)
    \]
\end{definition}

\begin{definition}[Start included]\label{def:pred_start_in}
    The start of $i_1$ falls within $i_2$:
    \[
        \mathit{StartOfFirstIntervalIn}(i_1,\, i_2) \;\Leftrightarrow\;
        \mathit{Start}(i_2) \leq \mathit{Start}(i_1) \leq \mathit{End}(i_2)
    \]
\end{definition}

\begin{definition}[End included]\label{def:pred_end_in}
    The end of $i_1$ falls within $i_2$:
    \[
        \mathit{EndOfFirstIntervalIn}(i_1,\, i_2) \;\Leftrightarrow\;
        \mathit{Start}(i_2) \leq \mathit{End}(i_1) \leq \mathit{End}(i_2)
    \]
\end{definition}

% DESIGN CONCLUSION: OPERATIONS AND TEMPORAL EXTENT
%
% Operations (e.g. calculate, fire) do NOT take t as an explicit argument.
% t is "ambient" -- it is supplied by the surrounding temporal construct
% (Always, Triggered, etc.) at the requirement level.
%
% An operation produces a point-in-time effect predicate that holds at the
% ambient t only. It says nothing about any other time point.
%
% We considered making calculate's effect hold from t onward
% (i.e. forall t' >= t: Val_rho(e, t') = expr), but rejected this because:
%   (a) The trace rho is a fixed mathematical object -- operations do not
%       "write" to it. It already has whatever values it has at every time point.
%   (b) A later operation in the same requirement may set a different value,
%       which would directly contradict a "forall t' >= t" clause.
%
% Temporal extent is always the responsibility of the surrounding temporal
% construct:
%   Always(calculate(e, expr))           -- holds at every t
%   Triggered(cond, calculate(e, expr))  -- holds at every t where cond fires
%   forall t' >= t: ...                  -- expressed at requirement level, not
%                                           inside the operation definition

\section{Operations}

\begin{definition}[Operation]\label{def:operation}
    An \emph{operation} is a function
    \[
        op : A_1 \times \cdots \times A_n \rightarrow \mathbf{Pred}
    \]
    for some $n \geq 0$ and argument types $A_i \subseteq V \cup E$.
    The predicate $op(a_1,\, \ldots,\, a_n)$ is the \emph{effect} of $op$ -- it describes the condition that the operation causes to hold at the ambient time $t$, which is supplied by the surrounding temporal construct.
    The trigger condition that invokes an operation is specified at the requirement level.
    We write $O$ for the set of all operations.
\end{definition}

\begin{definition}[Entity type operations function]\label{def:entity_type_ops_fn}
    \[
        O_T : T_E \times \mathcal{P}(M) \rightarrow \mathcal{P}(O)
    \]
    , maps each entity type and set of modifiers to its applicable operations.
    
    It holds that: $O = \bigcup_{\tau \in T_E,\, \mu \subseteq M} O_T(\tau,\, \mu)$.

    The concrete mapping, including argument types and effects, is as follows.

    \medskip
    \begin{tabularx}{\textwidth}{lXX}
        \toprule
        \textbf{Operation} & \textbf{Arguments}                                                                    & \textbf{Effect $op(args)$} \\
        \midrule
        $\mathit{calculate}(e,\, \mathit{expr})$
                           & Signal $e \in E$,\newline expression $\mathit{expr} \in V$
                           & $\mathit{Val}_\rho(e, t) = \mathit{expr}$                                                                          \\[4pt]
        \sout{$\mathit{read}(t,\, e,\, a)$}
                           & \sout{$t \in T$, Storage $e \in E$, address $a$}
                           & \sout{$\mathit{Cmp}(\mathit{Val}_\rho(e, t),\, =,\, \mathit{ValueAt}(a))$}                                         \\[4pt]
        \sout{$\mathit{store}(t,\, e,\, \mathit{dst})$}
                           & \sout{$t \in T$, Storage $e \in E$, address $\mathit{dst}$}
                           & \sout{$\mathit{Cmp}(\mathit{ValueAt}(\mathit{dst}),\, =,\, \mathit{Val}_\rho(e, t))$}                              \\[4pt]
        $\mathit{fire}(e)$
                           & Event $e \in E$
                           & $\mathit{Val}_\rho(e, t) = \mathit{true}$                                                                          \\[4pt]
        \sout{$\mathit{transmit}(t,\, e,\, v)$}
                           & \sout{$t \in T$, Channel $e \in E$, value $v \in V$}
                           & \sout{$\mathit{Cmp}(\mathit{Val}_\rho(e, t),\, =,\, v)$}                                                           \\[4pt]
        \sout{$\mathit{receive}(t,\, e,\, v)$}
                           & \sout{$t \in T$, Channel $e \in E$, value $v \in V$}
                           & \sout{$\mathit{Cmp}(\mathit{Val}_\rho(e, t),\, =,\, v)$}                                                           \\[4pt]
        \sout{$\mathit{set\_state}(t,\, e)$}
                           & \sout{$t \in T$, State $e \in E$}
                           & \sout{$\mathit{Cmp}(\mathit{Val}_\rho(e, t),\, =,\, \mathit{true})$}                                               \\[4pt]
        \sout{$\mathit{compare}(t,\, e_1,\, e_2)$}
                           & \sout{$t \in T$, Entities $e_1, e_2 \in E$}
                           & \sout{$\mathit{Cmp}(\mathit{Val}_\rho(e_1, t),\, =,\, \mathit{Val}_\rho(e_2, t))$}                                 \\
        \bottomrule
    \end{tabularx}
    \medskip

    % \noindent Here $\mathit{ValueAt}(a)$ denotes the value stored at memory address $a$.
\end{definition}

\section{Temporal constructs}

A \emph{trace predicate} is a function $P : \Sigma^T \to \mathbb{B}$.
A trace $\rho$ \emph{satisfies} a requirement $R$ (written $\rho \models R$) iff $R$'s constraint $P$ is such that $P(\rho) = \mathit{true}$.
The temporal constructs below build trace predicates by quantifying a predicate $\psi \in \mathbf{Pred}$ over time.

\begin{definition}[Always]\label{def:always}
    \[
        \mathit{Always}(\psi) \;\Leftrightarrow\; \forall t \in T:\; \psi(\rho, t)
    \]
\end{definition}

\begin{definition}[Eventually]\label{def:eventually}
    \[
        \mathit{Eventually}(\psi) \;\Leftrightarrow\; \exists t \in T:\; \psi(\rho, t)
    \]
\end{definition}

\begin{definition}[Initial]\label{def:initial}
    \[
        \mathit{Initial}(\psi) \;\Leftrightarrow\; \psi(\rho, 0)
    \]
\end{definition}

\begin{definition}[Triggered]\label{def:triggered}
    \[
        \mathit{Triggered}(\psi_{\mathit{cond}},\, \psi_{\mathit{effect}}) \;\Leftrightarrow\;
        \forall t \in T:\; \psi_{\mathit{cond}}(\rho, t) \Rightarrow \exists t' \geq t:\; \psi_{\mathit{effect}}(\rho, t')
    \]
\end{definition}

\begin{definition}[Triggered within]\label{def:triggered_within}
    \[
        \mathit{TriggeredWithin}(\psi_{\mathit{cond}},\, \psi_{\mathit{effect}},\, d) \;\Leftrightarrow\;
        \forall t \in T:\; \psi_{\mathit{cond}}(\rho, t) \Rightarrow \exists t' \in [t,\, t+d]:\; \psi_{\mathit{effect}}(\rho, t')
    \]
    where $d \in \Delta$ is a duration bound.
\end{definition}

The constructs compose via standard boolean operators on trace predicates:
conjunction $P_1 \land P_2$, disjunction $P_1 \lor P_2$, and negation $\lnot P$.

\section{Notes}

\begin{itemize}
    \item Lepsi ponechat typ entity, protoze musime mapovat na modifiers,
          item ktere jsou rozdilne pro kazdy typ entity. Zaroven muzeme namapovat
          typ entity na mnozinu operaci.
    \item Modifiers need to be parameterized.
    \item We could theoretically map entity types directly to predicates, which
          can be set to \textit{true}, but maybe we will want different predicates depending on the context (e.g. discrete or continuous time).
    \item $T$ should be merged discrete and continuous.
    \item Need to resolve undefined with time functions.
    \item Last n-th occurence.
    \item From time to predicates that hold? What would that be useful for? Do we really want to reference predicates in other predicates?
\end{itemize}

\end{document}